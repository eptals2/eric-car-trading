#RUN THIS:
    npm install -> install needed packages
    npm run dev -> if developing/run dev server
    npm install -g wrangler -> wrangler packages for cloudflare deployments
    npm run build
    wrangler deploy